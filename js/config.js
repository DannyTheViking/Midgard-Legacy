/* MIDGARD LEGACY - shared timer settings */

const TEST_MODE = true;

const HONEY_TIME_SECONDS = TEST_MODE
    ? 5 * 60
    : 12 * 60 * 60;

const YOUNG_MEAD_TIME_SECONDS = TEST_MODE
    ? 5 * 60
    : 24 * 60 * 60;